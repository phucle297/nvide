[package]
name = "nvide-platform"
description = "OS abstractions (IPC paths, local sockets) for NVide"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
thiserror = { workspace = true }
