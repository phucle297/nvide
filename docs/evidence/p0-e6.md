# P0-E6 formal presentation evidence

- Status: CANDIDATE — FINAL EVIDENCE REVIEW PENDING
- Workload commit: `6e156b605618389ae50b58d6ad4ec3eee0763bae`
- Prepared by: `agent:/root`
- Artifact: [`p0-e6-formal-6e156b6.zip`](p0-e6-formal-6e156b6.zip)
- Artifact SHA-256: `e731e169e6a4726f407cd45e6cbb62819873d57175b3b2f745b39c68600bcf37`
- Artifact size: 1,355,732 bytes
- Approved binding: [`p0-e6-binding.md`](p0-e6-binding.md)

The immutable ZIP contains the five accepted clear runs and one accepted edit run. Every capture manifest reports `PASS`, the exact workload commit, topmost physical-panel presentation at 120 Hz, PresentMon exit `0`, and the same harness, NVide, and PresentMon hashes. This record is not an approval; P0-E6 and Phase 0 exit require two independent final reviews.

## Bound environment

| Item | Value |
| --- | --- |
| Host | Physical Lenovo `83HY`; Windows 11 Pro `10.0.26200`; AMD Radeon 860M |
| Display | Lenovo `LEN8BAD`; 2880×1800; 120 Hz configured; 120.048019 Hz calibrated |
| Render path | 1920×1080 FIFO swapchain; Vulkan; `SyncInterval=1`; tearing disabled |
| Harness | `tools/phase0-presentmon.ps1`; SHA-256 `FE44E3728E65C2EE76E6D62DF4A54203F8925845D0D54C870920A0C423BDA490` |
| NVide binary | SHA-256 `D549E8A3B63E179F4282C208E3DE44E413FBF34DA12A75E73D70D87F5BA257CC` |
| Authority | PresentMon 2.5.1; SHA-256 `9BEC3083069F58F911E6A512F4806DB51A27BD096103087BC1D05EF54C80A191`; `--qpc_time --v1_metrics` |
| Refresh period | `R = 8,330,000 ns`; fixed 2 ms edit join is below `R / 4 = 2.0825 ms` |

The exact command template and full per-run arguments are retained in each `capture-manifest.txt`:

```powershell
cargo build --locked --release -p nvide -p nvide-core
powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools\phase0-presentmon.ps1 -Kind clear -RunId <formal-clear-id> -Output <fresh-absolute-directory> -NvideExe target\release\nvide.exe -PresentMonExe C:\Users\permees\AppData\Local\nvide-phase0\PresentMon-2.5.1-x64.exe -WarmupSeconds 10 -MeasureSeconds 30
powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools\phase0-presentmon.ps1 -Kind edit -RunId formal-6e156b6-edit-01 -Output <fresh-absolute-directory> -NvideExe target\release\nvide.exe -PresentMonExe C:\Users\permees\AppData\Local\nvide-phase0\PresentMon-2.5.1-x64.exe -WarmupEdits 10 -MeasureEdits 30
```

## Clear workload

For each run, displayed time in nanoseconds is `QPCTime × 100 + floor(msUntilDisplayed × 1,000,000)`. Only `Dropped=0` rows in the half-open runtime window `s <= t < e` count. With `N` sorted displayed timestamps, P0.2 defines `FPS = (N - 1) × 1e9 / (tN - t1)`, `expected = floor((e - s) / R)`, `missed = max(expected - N, 0) / expected`, and nearest-rank p99 intervals.

| Run | N / expected | Displayed FPS | Missed rate | p99 interval | Start / end edge | Authority completion | Result |
| --- | ---: | ---: | ---: | ---: | ---: | --- | --- |
| `clear-01` | 3600 / 3601 | 120.000185 | 0.000277701 | 9.2401 ms | 4.0077 / 4.3718 ms | natural exit `0`; drain 6,619 ms | PASS |
| `clear-02` | 3600 / 3601 | 119.996810 | 0.000277701 | 9.2360 ms | 4.2572 / 3.2789 ms | natural exit `0`; drain 6,619 ms | PASS |
| `clear-03` | 3600 / 3601 | 119.996719 | 0.000277701 | 9.2475 ms | 0.5578 / 6.9554 ms | natural exit `0`; drain 6,525 ms | PASS |
| `clear-04` | 3600 / 3601 | 119.996131 | 0.000277701 | 9.2333 ms | 0.9966 / 6.3696 ms | natural exit `0`; drain 6,628 ms | PASS |
| `clear-05` | 3600 / 3601 | 119.996552 | 0.000277701 | 9.2323 ms | 6.7206 / 0.7509 ms | natural exit `0`; drain 6,619 ms | PASS |

All five runs independently satisfy displayed FPS `>= 119`, missed rate `<= 0.005`, and both edge distances `<= R`.

## Edit workload

The artifact contains 40 compositor acknowledgements: 10 warmup plus 30 measured edits. `runtime.csv` contains exactly 30 measured rows. Every measured row has a unique and equal trace/version, a shaped sentinel, changed sentinel pixels, equal expected/actual frame sequence, a retained 1920×64 RGBA readback, and ordered dispatch → core receipt → version increment → viewport emit/receive → present → displayed timestamps.

| Interval | Median | Nearest-rank p95 | Phase 0 treatment |
| --- | ---: | ---: | --- |
| UI dispatch → core version increment | 0.9537 ms | 1.5483 ms | Diagnostic; below Architecture's 2 ms target |
| Viewport receipt → present call | 4.1541 ms | 6.3395 ms | Diagnostic; below Architecture's 8 ms target |
| UI dispatch → compositor display | 20.48935 ms | 27.8352 ms | Correlation evidence |

The complete retained PresentMon capture has 191 rows. The harness revalidated one displayed event for every requested sequence, observed NVide exit success, drained authority output for 1,889 ms, and then stopped the named edit session without changing any trace deadline or sample.

## Independent recalculation

Extract the artifact, verify its hash, then apply the formulas above directly to each `presentmon.csv` and `runtime.csv`:

```bash
sha256sum docs/evidence/p0-e6-formal-6e156b6.zip
unzip -t docs/evidence/p0-e6-formal-6e156b6.zip
unzip -q docs/evidence/p0-e6-formal-6e156b6.zip -d <fresh-directory>
```

The reviewer must also verify each `capture-manifest.txt` hash/commit/status, exact row counts and uniqueness, every edit timestamp invariant, sentinel/readback presence and size, clear-window edge coverage, and the P0.2 pass rules before issuing a verdict.
