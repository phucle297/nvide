{
  "version": "1.3.0",
  "type": "raw_tool_signal",
  "session_id": "01KY751GYYFSRC7E5R3EH6RVH8",
  "actor_name": "grok+H6RVH8",
  "actor_type": "worker",
  "id": "01KY7GE9CY4GF213GE52RSS522",
  "source": {
    "tool": "grok",
    "command": "PostToolUse"
  },
  "payload": {
    "phase": "post",
    "host": "grok",
    "tool": "search_replace",
    "tool_input": {
      "file_path": "/home/permees/Projects/github.com/phucle297/nvide/docs/assets/document.js",
      "old_string": "  function setActiveTocItem(current) {\n    if (!current) return;\n    if (lastActiveId === current.id) {\n      /* still toggle class in case of full re-render */\n    }\n    lastActiveId = current.id;\n    tocItems.forEach(function (item) {\n      item.link.classList.toggle(\"active\", item === current);\n    });\n    const link = current.link;\n    if (!sidebar || !link) return;\n    const linkRect = link.getBoundingClientRect();\n    const sideRect = sidebar.getBoundingClientRect();\n    if (\n      linkRect.top < sideRect.top + 8 ||\n      linkRect.bottom > sideRect.bottom - 8\n    ) {\n      link.scrollIntoView({ block: \"nearest\", inline: \"nearest\" });\n    }\n  }",
      "new_string": "  function setActiveTocItem(current) {\n    if (!current) return;\n    const same = lastActiveId === current.id;\n    lastActiveId = current.id;\n    tocItems.forEach(function (item) {\n      item.link.classList.toggle(\"active\", item === current);\n    });\n    if (same) return;\n    const link = current.link;\n    if (!sidebar || !link) return;\n    const linkRect = link.getBoundingClientRect();\n    const sideRect = sidebar.getBoundingClientRect();\n    if (\n      linkRect.top < sideRect.top + 8 ||\n      linkRect.bottom > sideRect.bottom - 8\n    ) {\n      link.scrollIntoView({ block: \"nearest\", inline: \"nearest\" });\n    }\n  }"
    },
    "tool_response": {
      "type": "SearchReplace",
      "EditsApplied": {
        "old_string": "  function setActiveTocItem(current) {\n    if (!current) return;\n    if (lastActiveId === current.id) {\n      /* still toggle class in case of full re-render */\n    }\n    lastActiveId = current.id;\n    tocItems.forEach(function (item) {\n      item.link.classList.toggle(\"active\", item === current);\n    });\n    const link = current.link;\n    if (!sidebar || !link) return;\n    const linkRect = link.getBoundingClientRect();\n    const sideRect = sidebar.getBoundingClientRect();\n    if (\n      linkRect.top < sideRect.top + 8 ||\n      linkRect.bottom > sideRect.bottom - 8\n    ) {\n      link.scrollIntoView({ block: \"nearest\", inline: \"nearest\" });\n    }\n  }",
        "new_string": "  function setActiveTocItem(current) {\n    if (!current) return;\n    const same = lastActiveId === current.id;\n    lastActiveId = current.id;\n    tocItems.forEach(function (item) {\n      item.link.classList.toggle(\"active\", item === current);\n    });\n    if (same) return;\n    const link = current.link;\n    if (!sidebar || !link) return;\n    const linkRect = link.getBoundingClientRect();\n    const sideRect = sidebar.getBoundingClientRect();\n    if (\n      linkRect.top < sideRect.top + 8 ||\n      linkRect.bottom > sideRect.bottom - 8\n    ) {\n      link.scrollIntoView({ block: \"nearest\", inline: \"nearest\" });\n    }\n  }",
        "tool_output_for_prompt": "The file /home/permees/Projects/github.com/phucle297/nvide/docs/assets/document.js has been updated successfully.",
        "tool_output_for_prompt_concise": "The file /home/permees/Projects/github.com/phucle297/nvide/docs/assets/document.js has been updated.",
        "absolute_path": "/home/permees/Projects/github.com/phucle297/nvide/docs/assets/document.js",
        "edits": {
          "details": [
            {
              "old_string": "  function setActiveTocItem(current) {\n    if (!current) return;\n    if (lastActiveId === current.id) {\n      /* still toggle class in case of full re-render */\n    }\n    lastActiveId = current.id;\n    tocItems.forEach(function (item) {\n      item.link.classList.toggle(\"active\", item === current);\n    });\n    const link = current.link;\n    if (!sidebar || !link) return;\n    const linkRect = link.getBoundingClientRect();\n    const sideRect = sidebar.getBoundingClientRect();\n    if (\n      linkRect.top < sideRect.top + 8 ||\n      linkRect.bottom > sideRect.bottom - 8\n    ) {\n      link.scrollIntoView({ block: \"nearest\", inline: \"nearest\" });\n    }\n  }",
              "old_line": 117,
              "new_string": "  function setActiveTocItem(current) {\n    if (!current) return;\n    const same = lastActiveId === current.id;\n    lastActiveId = current.id;\n    tocItems.forEach(function (item) {\n      item.link.classList.toggle(\"active\", item === current);\n    });\n    if (same) return;\n    const link = current.link;\n    if (!sidebar || !link) return;\n    const linkRect = link.getBoundingClientRect();\n    const sideRect = sidebar.getBoundingClientRect();\n    if (\n      linkRect.top < sideRect.top + 8 ||\n      linkRect.bottom > sideRect.bottom - 8\n    ) {\n      link.scrollIntoView({ block: \"nearest\", inline: \"nearest\" });\n    }\n  }",
              "new_line": 117,
              "context_before": "    return (header ? header.offsetHeight : 52) + 16;\n  }\n\n",
              "context_after": "\n  function sectionBounds(i) {\n    const start = tocItems[i].el.getBoundingClientRect().top;\n",
              "line_prefix": ""
            }
          ]
        }
      }
    },
    "text": "tool search_replace → /home/permees/Projects/github.com/phucle297/nvide/docs/assets/document.js",
    "path": "/home/permees/Projects/github.com/phucle297/nvide/docs/assets/document.js",
    "command": null
  }
}